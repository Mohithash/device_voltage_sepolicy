# ATV
# used by mediashell
ro.odm.cast.ssid_suffix u:object_r:odm_cast_prop:s0

# Bluetooth
bluetooth.hci.disabled_commands        u:object_r:bluetooth_config_prop:s0
bluetooth.hci.erroneous_data_reporting.enabled        u:object_r:bluetooth_config_prop:s0

# Camera
persist.vendor.camera.privapp.list     u:object_r:camera2_extensions_prop:s0
vendor.camera.aux.packageexcludelist   u:object_r:camera2_extensions_prop:s0
vendor.camera.aux.packagelist          u:object_r:camera2_extensions_prop:s0
vendor.camera.skip_unconfigure.packagelist  u:object_r:camera2_extensions_prop:s0

# Custom version
ro.bestrom.device                      u:object_r:custom_version_prop:s0
ro.bestrom.version                     u:object_r:custom_version_prop:s0

# Dolby Properties
ro.vendor.dolby.dax.version            u:object_r:exported_system_prop:s0
ro.vendor.audio.dolby.dax.support      u:object_r:exported_system_prop:s0
ro.vendor.audio.dolby.dax.version      u:object_r:exported_system_prop:s0
ro.vendor.audio.dolby.surround.enable  u:object_r:exported_system_prop:s0

# Force multi-window behavior
ro.config.force_multi_window           u:object_r:exported_config_prop:s0 exact bool

# NFC
persist.vendor.nfc.config_file_name    u:object_r:vendor_persist_nfc_prop:s0 exact string
persist.vendor.nfc.fw_file_name        u:object_r:vendor_persist_nfc_prop:s0 exact string

# Recovery battery
ro.recovery.batteryless                u:object_r:recovery_config_prop:s0 exact bool

# Recovery filesystem
ro.recovery.supported_fs               u:object_r:recovery_config_prop:s0 exact string

# Recovery input
ro.minui.blacklist_input_devices       u:object_r:recovery_config_prop:s0

# Recovery touch
ro.minui.default_touch_rotation        u:object_r:recovery_config_prop:s0

# Recovery update
persist.vendor.recovery_update         u:object_r:recovery_update_prop:s0

# Resolution switch
ro.bestrom.display.resolution_switch  u:object_r:exported_system_prop:s0

# xtra-daemon control
persist.sys.xtra-daemon.enabled        u:object_r:xtra_control_prop:s0 exact bool

# Screen mirroring disablement
persist.sysui.disable_mirroring_confirmation_dialog u:object_r:hdmi_config_prop:s0

# USB
ro.usb.uvc.disable_video_encode_flag   u:object_r:usb_uvc_config_prop:s0
vendor.usb.uvc.payload_transfer_size   u:object_r:usb_uvc_config_prop:s0

# Themed Bootanimation dynamic colors
persist.bootanim.color1     u:object_r:bootanim_system_prop:s0
persist.bootanim.color2     u:object_r:bootanim_system_prop:s0
persist.bootanim.color3     u:object_r:bootanim_system_prop:s0
persist.bootanim.color4     u:object_r:bootanim_system_prop:s0

# Global cleartext feature
persist.sys.global.cleartext           u:object_r:system_exported_cleartext_prop:s0 exact int

# Compact HUN
persist.sys.compact_heads_up_notification.always_show          u:object_r:system_prop:s0

# Nullroute — property context fragment.
#
# Appended from packages/apps/Nullroute/rom/sepolicy/property_contexts.frag.
#
# Both entries end in a dot, which makes them prefix matches.
#
#   persist.sys.nullroute.mode         0 ENFORCE | 1 PAUSED | 2 OFF.
#                                      THE on-disk source of truth for
#                                      pause/enforce; NrControl::mode is derived
#                                      from it by the syncprop trigger. One
#                                      authority, so two switches can never
#                                      disagree.
#   persist.sys.nullroute.kill         1 = app-independent kill switch. The
#                                      resolver returns PASS for everything and
#                                      H4 stops skipping the L0 hosts scan. This
#                                      is the recovery path when the app cannot
#                                      be launched at all.
#   persist.sys.nullroute.fail_streak  boot-loop breaker counter (F7). Armed by
#                                      nullroute_seed at every post-fs-data,
#                                      cleared by the app after 120 s of healthy
#                                      uptime, quarantines the index at 3.
#   persist.sys.nullroute.deep         Deep mode (Phase 4) enable.
#
#   sys.nullroute.filter               written by netd at every state change:
#                                      ok:<gen> | nomap:<errno> | badhdr:<field>
#                                      | killed | off. Health signal #2 of
#                                      three, and the only one that is
#                                      independent of both the control page and
#                                      the app being alive (F8).
#   sys.nullroute.seed                 written by nullroute_seed: ok |
#                                      recompiled | quarantined:<reason> |
#                                      failed:<errno>.
#
# ro.nullroute.enabled is intentionally NOT listed. It is a build-time property
# shipped from rom/nullroute.mk and it falls through to default_prop, which is
# world-readable — correct for a static feature flag that vendor code may want
# to read, and it must not be system_restricted_prop or init could not publish
# it from build.prop in a context every reader can see.

persist.sys.nullroute.    u:object_r:nullroute_prop:s0
sys.nullroute.            u:object_r:nullroute_state_prop:s0
